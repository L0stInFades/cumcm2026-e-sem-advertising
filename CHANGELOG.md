# 变更记录

格式遵循 Keep a Changelog；版本号遵循语义化版本。

## [1.0.0] - 2026-09-12

本条目合并了首次发布与随后按三位审稿人（rigor / compliance / writing）意见做的修订；逐条处理记录见 `docs/REVIEW_RESPONSE.md`。

### Fixed（审稿修订，全部在 run `20260912-041501-ae773aa` 内重跑）

- **灵敏度设计的机制耦合（严重）**：`_sensitivity` 的“最小投放额 $m=0$”情形此前在放松下界的同时让式 (17) 上限中的 $\max\{\cdot,m\}$ 消失，可行域实际变小，导致“更弱的约束下最优值反而下降”的单调性违反，并被误读为“选择机制的目标代价”。现新增 `caps_as_baseline` 选项把上限固定为基准问题的有效上限（真松弛），两行并列给出；最小投放额的真实目标代价为 0.12%（8 月）与 0.02%（2 月），而非此前隐含的 2.3 个百分点。新增自动门禁 `sensitivity_monotonicity.json`：任何名义松弛情形的最优值低于基准即阶段失败（MDR-0013）。
- **问题四缺最优性证书**：两层（跨日 → 日内）分解此前被声称“日内上限不起作用时精确”，而该条件在本次运行中被违反（日内上限经常起作用）。现对每个单元把“7 日 × 关键词”的联合问题作为可分离凹松弛求解（去下界、上限取 $\max\{\bar x_i(B_u),m\}$ 这一最宽的盒子——上限必须做 $\max\{\cdot,m\}$ 的抬升才是有效上界），给出联合最优性界并写入表 13 与论文 §7.2、§8.6。
- **部署区间从未被回测**：`rolling_backtest` 新增 `calendar_wide` 模型，用与 2026 年预测完全相同的 $\sqrt{\sigma_u^2+(\sigma^m_u)^2}$ 宽度回测；结果显示部署区间过覆盖（约 87%–92%）而残差区间接近名义 80%。$\sigma^m$ 被明确写成不可检验的建模约定（假设 A8、§9.2）。
- **式 (5) 的规范结论只有点估计**：新增 `eda.holiday_optimal_cut`，对 $(\delta',\theta)$ 的 HC3 协方差作联合蒙特卡罗，给出最优减投幅度的 95% 区间、临界弹性 $\theta^*$ 与在合并点击弹性下的反号结果；结论改为“数据不能判定假日减投是过度还是不足”，并对假日/周一/周日统一判定标准。
- **§6.2 的可行性论证**：历史比例点对含最小投放额的问题并不可行（其正坐标 87.5% 低于 $m$），“最优值 ≥ 基线值”不能先验推出；改为经验结论并逐一核对（107/107，最小比值 1.0062，`baseline_feasibility.json`）。
- **摘要与假设中的弹性区间**：$[0.6502,0.9301]$ 是模型采用值而非回归估计；改为报告回归估计 $\hat\gamma_u$ 的范围、显著小于/大于 1 的单元数与 $H_0:\gamma=1$ 的检验，并在假设 A4 中显式给出 $\hat\gamma>1$ 的反例及其处理。
- **2026-09-13/14 的星期标注**（周六、周日 → 周日、周一）；“最优解用尽至分以内” → “至 0.3 元以内”；“约三分之一的假日跌幅来自需求侧” → 对数尺度 21%（百分点不可加）；“展位指数 2.6126 介于第 2 与第 3 位之间” → 报告其背后的三点混合结构并说明 $\pi$ 是名次的乐观下界。
- **附录 B 的复现命令在 PDF 文本层丢空格**：改用 `verbatim` 排版；`pipelines/common/stages.py` 对“正在渲染本表的阶段”显式标注，`REPRODUCE.md` 的 `<科学阶段...>` 占位符由 `_science_stages()` 展开，两份复现记录因此一致（平台代码改动）。
- **符号重载**：adstock 衰减 $\eta\to\lambda_{\mathrm{ad}}$、关键词特异冲击 $\eta_i\to\xi_i$、效益指数 $B_i\to\mathcal B_i$、日冲击源 $z_t\to w_t$；补入 $K_u$、$\bar X_u$、$\mathcal K^e_u$、$\sigma^{\mathrm{kw}}_u$、$\zeta_i$ 的定义与 $\hat\gamma_u$/$\gamma_u$ 的区分。

### Added（审稿修订）

- `eda.attribution_diagnostics`：$r_u$ 的移动分块残差自助区间、非负边界标记、月份哑变量设定摆动、残差化设计阵条件数，以及恰好相加为 85,313 的“截距 / 日历 / SEM”分解；表 4 增列原始 NNLS、自助区间与活跃天数 $n_u$。
- `classify.axis_diagnostics`：两个坐标轴的 Pearson/Spearman 相关（0.93/0.90）、各类的消费与点击占比、效率口径的对照分类；注意力分量改用总停留时间的口径稳健性。表 7 新增两行。
- 灵敏度新增 4 种情形（$\kappa=1$、$\kappa$ 截断 $[0.5,2]$、重复词只留最优副本、最小投放额 0 且上限同基准），共 14 种。
- 问题三新增：跨单元预算再分配的反事实（`_cross_unit_allocation`，异质弹性下对影子价格二分）、2 月窗口覆盖全部 12 个单元的备选方案（`plan_feb_annual_avg.csv`）、跨单元重复投放的暴露规模（`duplication_exposure.json`）、有符号校准偏差与单元–日水平偏差的重标定不变性检验。
- 问题四新增：全年日均预算口径的完整逐日逐词方案（`plan_annual_alt.csv`）、`result4.xlsx` 中 2 个零预算单元的零行（使全部 12 个单元出现，行数 845 → 859）、点击加权的上方位/首位占比、蒙特卡罗标准误、情景平均 CPC。
- MDR-0012（问题四预算口径、单元覆盖与零行）、MDR-0013（灵敏度的机制耦合与单调性门禁）；MDR-0001/0003/0004/0006/0008 更新。
- `docs/REVIEW_RESPONSE.md`：三位审稿人全部意见的逐条处理记录，含 4 条未采纳意见的理由。
- 单元测试：跨单元分配的边际均衡与预算用尽、带下界的解在真松弛下的单调性、联合松弛是两层解的上界。


### Added
- 问题四补齐题目要求的**六个量**（竞价/CPC、展现量、展现位、点击量、浏览量、注册量）的期望范围：新增正文表 `tab_q4_ranges`（7 日合计期望 + 10%--90% 范围 + 相对宽度，以及单关键词--日的中位期望与中位范围），新增登记数字 `QfourPositionLow`/`QfourPositionHigh`（点击加权的展位指数分位），逐日表 `tab_q4_daily` 补齐“浏览范围”与“展位范围”两列，使表题与内容一致（此前展现位只有点估计，逐日表的表题与内容不符）。
- 建模决策记录 MDR-0011：六个量的可观测代理（竞价 → CPC，展现位 → 排位指数 $\pi$）、报告粒度（合计 + 单关键词--日）与区间的聚合规则；论文第 7.3、8.6、9.2 节写明该规则并把它列为局限。

### Changed
- `manuscript/main.tex`：浮动页的 `\@fptop`/`\@fpsep`/`\@fpbot` 改为把浮动体顶到版心上沿（余量落在页底），并放宽 `topfraction` 等浮动参数——消除了表 4/图 5、表 6/图 7、表 11/图 12 之间的大片空白。
- `tab_q4_ranges` 的合计区间与登记数字（`\val` 宏）同源：由 `plan_by_unit.json` 生成，单元内 7 天按同一情景对齐求和后取分位，单元之间把分位数相加；首版实现按单元--日相加，区间显著偏宽且与正文宏不一致，已修正。

### Verified
- run `20260912-041501-ae773aa`（同一 run 内重跑 forecast、results、figures、tables、lint、test、paper、qa）：44 项测试通过；qa 15 项检查全部通过。

## [0.9.0] - 2026-09-12

### Added（论文）
- 论文全文（摘要 1 页，正文 25 页，附录含支撑材料清单、复现记录、补充表格与完整程序）：问题重述、问题分析（TikZ 技术路线）、编号假设与符号表、四个问题的模型建立与求解（注水解定理及证明、两阶段拉格朗日–注水算法、确定性等价命题）、模型检验（独立校验器、凸松弛最优性界、校准、灵敏度、滚动回测）、评价与推广、AI 使用声明；参考文献 21 条（GB/T 7714 数字式）。
- `manuscript/main.tex`：`\papertable` 宏（生成的三线表按需缩放到版心宽度）、`gbt7714` 数字式引用、附录"补充表格"。

### Verified
- run `20260912-041501-ae773aa`：论文撰写阶段内重跑 eda、classify、allocate、forecast、results、figures、tables、lint、test（44 项测试通过），paper/qa 15 项检查全部通过（摘要 1 页、正文 25 页、无未定义引用、字体全嵌入、Overfull ≤ 3.7 pt、无缺字、result2/3/4.xlsx 契约通过），package 262 个成员无漂移，release 散列本地核对一致。
- 身份信息观察名单复核：5 处命中仍全部位于附录代码清单对 `configs/default.toml` 中 `identity_watchlist` 定义的转录，不含真实身份信息。

### Fixed (论文撰写阶段，run `20260912-041501-ae773aa` 内重跑全部科学阶段与门禁)
- `eda`：归因注册改按下游实际采用的注册率（单元估计或合并回退）计算，原始 NNLS 估计保留为 `attributed_regs_raw`；此前使用合并回退的单元在表中显示归因注册 0、注册成本发散（MDR-0010）。
- `forecast.simulate_group`：日效率冲击按 $\exp(\mu+\sigma z)$ 抽样，使情景均值等于分配所用的对数正态期望 $\exp(\mu+\sigma^2/2)$；此前多减了 $\sigma^2/2$，蒙特卡罗期望注册（1101）系统性低于确定性目标（1187）。修正后二者一致（MDR-0008 补充）。
- `tables`：新增 `raw` 单元格规格，`tab_backtest` 的数学表头与 `tab_verification` 的 `\%` 不再被双重转义；`tab_attribution_response` 同时给出单元估计 $\hat\gamma_u$ 与采用值；`tab_verification` 报告凸松弛总体最优性界，并把预算不足 20 倍最小投放额的微小问题单列。
- `figures`：`fig_allocation_mix`（两窗口均为"重点词 ≈ 99%"，无信息量）替换为 `fig_allocation_shift`（最优窗口日均投放 vs 2025 年日均消费的双对数散点 + 各类候选/入选计数）。
- `stages`：新增论文所需的登记数字（单元/方案数、品牌型与最低 CTR 单元、科学计数法 p 值、控制消费后的周几 p 值、CTR/上方位假日效应、Mann–Whitney 中位数、最优假日减投比、上/非上方位 CTR、零消费词占比、重复词行数、凸松弛微小问题数与信息量充分的最大相对差、达上限/最小投放额词–日数、8 月窗口单元提升极值、全年日均预算口径的注册增幅等）。

### Added
- 科学阶段：`eda`（问题一：KPI、集中度、日历/假日回归、注册归因、弹性）、`classify`（问题二：熵权效益指数 + 自然断点分类与独立复核）、`allocate`（问题三：两阶段拉格朗日/注水求解的可分离凹规划、独立最优性审计、反事实对照、自助区间、灵敏度、窗口预算备选）、`forecast`（问题四：日历回归预测与滚动回测、两层随机分配、蒙特卡罗期望范围）、`results`、`figures`、`tables`。
- 建模决策记录 MDR-0001…0009；`docs/DATA_NOTES.md`；`docs/RESULTS.md`。
- 单元/性质测试：日历与解析、分类规则与校验器、分配求解器（网格穷举、SLSQP 子集枚举、性质测试）、预测与模拟。

### Verified
- run `20260912-041501-ae773aa`（Modal，medium 档）：ingest、validate、eda、classify、allocate、forecast、results、figures、tables、lint、test、paper、qa 全部 completed；44 项测试通过；qa 中 result2/3/4.xlsx 契约检查通过（`--param require_results=true`），论文用骨架编译。
- Q3：107 个单元-日分配问题全部通过独立审计，凸松弛总体最优性界 0.080%；Q4：75 个分配问题全部通过审计。
- 身份信息观察名单复核：qa 报告 5 处命中全部位于附录代码清单第 19 页对 `configs/default.toml` 中 `identity_watchlist` 定义本身的转录（"大学、学院、赛区、队号、指导教师" 字面量），不含任何真实身份信息；论文骨架、结果文件与支撑材料均不含姓名、学校、赛区、队号、邮箱。

### Changed
- `pyproject.toml`：`[tool.ruff.lint.pycodestyle] max-line-length = 150`——E501 按显示宽度计数（中文字符计 2），论文用中文字符串需要余量；格式化宽度仍为 120。
- `configs/default.toml`：`paper.sources = ["figures", "tables"]`，`package.stages` 加入四个科学阶段；新增 `[classify]`、`[allocate]`、`[forecast]` 参数节。

## [0.1.0] - 2026-09-12

### Added
- Forge 云端运行时（阶段/运行/清单模型、数据与结果契约、事件日志、确定性打包）。
- 通用流水线：ingest、validate、fmt、lint、test、paper、qa、package、release；全部在 Modal 执行。
- CUMCM 格式论文骨架（XeLaTeX + BibTeX gbt7714），自动生成数字宏、支撑材料清单、复现记录与完整程序附录。
- 工程规范、建模与写作标准、运行手册、ADR/MDR、AI 使用记录、Unlicense。

### Verified
- 三题仓库全链路冒烟通过：25 项单元/性质/集成测试，13 项论文与结果 QA 检查，发布散列核对一致。
