"""Forge — cloud-only research engineering runtime (CUMCM 2026).

Every computation (data parsing, modelling, solving, tests, figures, LaTeX, PDF QA,
packaging) runs inside Modal. The local machine only edits text, drives git,
submits jobs, downloads artifacts and verifies transport hashes.
"""

__version__ = "1.0.0"
